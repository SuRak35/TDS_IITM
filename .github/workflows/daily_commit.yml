name: Daily Commit

on:
  schedule:
    # Runs at 9:00 AM UTC every day
    - cron: "0 9 * * *"
  workflow_dispatch: # Allow manual triggers

jobs:
  daily_commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Git
        run: |
          git config --local user.email "24ds1000046@ds.study.iitm.ac.in"
          git config --local user.name "DevSync Automation Bot"

      - name: Create or update a file
        run: |
          # This will append the current date and time to a file named status.txt
          echo "Commit made on $(date)" >> status.txt
          git add status.txt
          git diff --cached  # To check if there are any changes

      - name: Commit the changes
        run: |
          git commit -m "Daily commit for automated update" || echo "No changes to commit"
          
      - name: Push changes
        run: |
          git push
